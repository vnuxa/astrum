local helper = {
	widgets = require("astrum.helper.widgets"),
	services = require("astrum.helper.service"),
	utils = require("astrum.helper.utils"),
	animations = require("astrum.helper.animations"),
}

return helper
